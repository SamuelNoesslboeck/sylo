[package]
name = "sylo"
version = "0.1.0"
edition = "2021"

[dependencies]
embedded-hal = { version = "0.2.7", features = [ "unproven" ] } 
serde = { version = "1.0.192", optional = true }

[features]
floats = [ ]
serde = [ "dep:serde" ]